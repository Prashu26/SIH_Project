# Copy this file to .env and fill in values. Do NOT commit .env (it's ignored).

# Server
PORT=8080
API_URL=http://localhost:8080
FRONTEND_URL=http://localhost:3000

# MongoDB (example local URI)
MONGODB_URI=mongodb+srv://prashugowda179_db_user:e6r64i4grlSD5yxd@cluster0.antybto.mongodb.net/?appName=Cluster0

# SMTP (for sending certificate emails)
# If using Gmail, create an App Password and use it as SMTP_PASSWORD
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ksrujan026@gmail.com
SMTP_PASSWORD=ounmbtbgricophvc
SMTP_FROM="Issuing Institution <no-reply@example.com>"

# Optional: gridfs bucket name (used to store artifacts)
CERTIFICATE_BUCKET_NAME=certificateArtifacts

# Optional: blockchain/anchor settings
PRIVATE_KEY=
RPC_URL=
CONTRACT_ADDRESS=
# Server
PORT=8080
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/legitdoc

# JWT / Auth
JWT_SECRET=change_me_in_production
JWT_EXPIRE=30d

# Polygon / Smart Contract Anchoring
POLYGON_RPC_URL=https://rpc-amoy.polygon.technology
CONTRACT_ADDRESS=0xYourLegitAnchorContract
ISSUER_PRIVATE_KEY=0xyour_private_key_with_issuer_rights
ISSUER_WALLET_ADDRESS=0xIssuerWallet

# IPFS (optional). Set USE_IPFS=true to upload PDFs to IPFS
USE_IPFS=false
IPFS_ENDPOINT=http://127.0.0.1:5001

# Issuer / Verification URLs
VERIFICATION_BASE_URL=https://your-frontend.example.com
API_BASE_URL=http://localhost:8080

# Logging
LOG_LEVEL=info
